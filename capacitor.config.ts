import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reacttest.com',
  appName: 'react-capacitor',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
